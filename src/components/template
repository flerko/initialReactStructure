import React from 'react';
import PropTypes from 'prop-types';

import './style.css'

export default class _Name extends React.Component {
    render() {
        const { className } = this.props;
        return(
            <div className={`class-name ${className}`}>

            </div>
        );
    }
}
_Name.propTypes = {
    className: PropTypes.string
};

_Name.defaultProps = {

};
